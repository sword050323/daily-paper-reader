Title: XPR: An Extensible Cross-Platform Point-Based Differentiable Renderer

URL Source: https://arxiv.org/pdf/2606.11529v1

Published Time: Thu, 11 Jun 2026 00:21:50 GMT

Number of Pages: 13

Markdown Content:
# XPR: An Extensible Cross-Platform Point-Based Differentiable Renderer 

## Steve Rhyner ∗1,2 Sankeerth Durvasula ∗1,2 Aleksandr Kovalev 1 Hansel Jia 1 Adrian Zhao 1,2

## Mrutunjayya Mrutunjayya 3 Nilesh Ahuja 3 Selvakumar Panneer 3

## Christina Giannoula 4 Nandita Vijaykumar 1,2

> 1

University of Toronto 2Vector Institute 3Intel 4Max Planck Institute for Software Systems 

## Abstract 

Point-based differentiable rendering underpins modern 3D re-construction, novel-view synthesis, and learning-based graphics pipelines, but developing new rendering methods often requires extensive low-level implementation, hardware-specific kernels, and manually written backward passes. This limits rapid prototyping, reproducibility, exploration, and deployment, especially across di-verse hardware platforms. This paper presents XPR, an extensible cross-platform framework for point-based differentiable rendering. XPR introduces a high-level programming interface that separates method-specific logic from the shared rendering pipeline, allowing users to implement new methods in a few lines of code. Its pipeline decomposes rendering into modular, statically shaped parallel op-erations that can be lowered by a cross-platform compiler to GPUs, TPUs, CPUs, and other ML accelerators. We demonstrate imple-mentations of 3DGS, 3DGUT, and LinPrim, with only a few 100s lines of Python code, each of which can be compiled to a range of hardware platforms with the XLA compiler. These results show that XPR enables fast experimentation and portable execution for emerging point-based differentiable rendering systems. 

## 1 Introduction and Motivation 

Differentiable rendering has become a core tool for recovering 3D scenes from images, with applications in 3D reconstruction [ 34 ], novel view synthesis [ 24 ], medical imaging [ 8], and 3D asset generation [ 41 ]. Among differentiable rendering methods, point-based scene representation methods, such as 3D Gaussian Splatting (3DGS) [ 24 ], model 3D scenes using spatial primitives defined by a 3D point, color, and density, and have become dominant due to their ability to render high-quality images at interactive frame rates. Differentiable rendering techniques and their applications have become an active area of research and development in academia and industry. Since 3DGS [ 24 ], point-based rendering has evolved into a rapidly expanding design space, with recent works explor-ing new primitive types [ 16 , 52 ], projection and pixel color/feature computation [ 54 ], rendered features such as depth [ 58 ] and se-mantics [ 43 ], rasterization strategies [ 44 ], and optimizations that improve efficiency and scalability [ 10 , 42 ]. Point-based renderers are also increasingly used as differentiable operators inside larger learning systems, including training pipelines for generative mod-eling [49], camera pose optimization [32], and robotics [29]. More recently, world-model and video-generation pipelines use differen-tiable rendering to explicitly enforce geometric consistency during training [13, 19, 40, 50]. 

> ∗

Both authors contributed equally to this paper. 

Table 1. Comparison of point-based differentiable rendering systems.                                                                                                                                                                                              

> System Implementation Programmability Portability
> Pt Var. HL Dec. AD NV AMD Int TPU Trn CPU 3DGS rast. [24] CUDA ✓✗✗✗✗✓G#✗✗✗✗
> gsplat [56] CUDA/HIP ✓G#G#G#✗✓✓✗✗✗✗
> OpenSplat [51] CUDA/HIP ✓✗✗✗✗✓✓✗✗✗✓
> Nvdiffrast [27] CUDA ✗✗G#G#✗✓✗✗✗✗✗
> PyTorch3D [45] CUDA ✓G#G#G#✗✓✗✗✗✗G#
> Kaolin [12] CUDA ✗✗G#✗✗✓✗✗✗✗✗
> Mitsuba3 [20] Dr.Jit ✗✗G#G#✓✓✗✗✗✗✓
> slang-gaussian-rast. [26] Slang/SLANG.D ✓✗✗✗✓✓✗✗✗✗✗
> taichi-splatting [5] Taichi ✓✗✓✗✓✓G#G#✗✗✓
> Brush [7] Rust/WGSL/Burn ✓✗✗✗✗✓G#G#✗✗✗
> Dressi [48] Vulkan-Compute ✗✗✗✗✓✓G#G#✗✗✗
> 3DGS.cpp [47] Vulkan-Compute ✓✗✗✗✗✓G#G#✗✗✗
> vkgs [39] Vulkan-Graphics ✓✗✗✗✗G#G#G#✗✗✗
> vulkan_gaussian_splat. [35] Vulkan-Graphics ✓G#✗✗✗G#G#G#✗✗✗
> Ours JAX/XLA ✓✓✓✓✓✓✓✓✓✓✓
> Legend. Pt: point-based rendering framework. Var.: ability to change the primitive, projection, or evaluation rule without rewriting the renderer. HL: high-level user code. Dec.: method-specific renderer logic decoupled from scheduling, tiling, and hardware mapping. AD: automatic differen-tiation for renderer code; hand-written backward kernels wrapped by host autograd are marked unsupported. NV, AMD, Int, TPU, Trn, and CPU denote NVIDIA GPU, AMD GPU, Intel GPU, Google TPU, Amazon Trainium, and CPU. ✓: supported. G#: partial or restricted. ✗: unsupported.

These trends expose a gap in existing point-based differentiable rendering frameworks. First, to enable quick exploration, prototyp-ing, and code reproduction , new methods should be easy to express by changing only the method-specific logic: the primitive repre-sentation, projection rule, pixel color evaluation, rendered features, or optimization strategy. The rest of the rendering pipeline—e.g., visibility filtering, tiling, sorting, blending, scheduling, and differ-entiation—should be reusable across methods. In practice, however, existing renderers often entangle these concerns in specialized low-level kernels, so each new variant requires substantial reimple-mentation and, in many cases, manually derived backward passes. Second, when used within a larger learning system, they inherit a 

portability requirement: because gradients must flow through the renderer at every training step, the renderer must execute on the same accelerator as the rest of the pipeline. As training infrastruc-ture expands to non-NVIDIA GPUs, Google TPUs, AWS Trainium, and other accelerators, these renderers must execute on the same accelerator and within the same compiler stack. These limitations define two technical challenges. (1) Extensi-bility. For ease of programming, a programmer should be able to introduce a new primitive, change how primitives are projected onto the screen, modify how a primitive contributes to a pixel’s color, or add new rendered features (e.g., depth, semantics) by spec-ifying only the new logic. None of these changes should require reworking tiling, parallelization across primitives and pixels, data layout, or the backward pass. Existing frameworks have limitations 

> arXiv:2606.11529v1 [cs.GR] 10 Jun 2026

in programming ease that are summarized in Table 1: Most are written in CUDA or Vulkan, require thousands of lines per method, and demand manually derived backward passes. The programmer must often reason about parallelization, intermediate data move-ment, and tile-based scheduling (Dec). gsplat [ 56 ] provides fast implementations of 3DGS on both NVIDIA and AMD GPUs, but it requires significant CUDA/HIP code changes when extending to any new method. slang-gaussian [ 26 ] and Taichi-splatting [ 5] raise the level of abstraction, but both still require significant codebase changes for a new method (Var). 

(2) Mapping parallelism across compute hardware. To map a single rendering framework with high-level programming abstrac-tions across hardware platforms, we must expose parallelism in a hardware-agnostic form that a cross-platform compiler can map to diverse architectures—threads and warps on GPUs, static-shaped tensor operations on TPUs [ 21 ] and Trainium [ 2, 3]. Most existing point-based renderers are written for specific hardware, e.g., using CUDA for NVIDIA GPUs, or HIP for AMD GPUs (summarized in Table 1). Vulkan- and WebGPU-based systems [ 7, 35 , 39 , 47 , 48 ]broaden compatibility to consumer GPUs with graphics pipelines beyond NVIDIA, but are currently incompatible with data center GPUs as they do not support Vulkan graphics drivers. Thus, they cannot be integrated with larger ML systems, e.g., generative/world models, on non-NVIDIA data center GPUs or accelerators like the TPU and Trainium. Furthermore, Vulkan-based systems comprise low-level kernels that do not provide abstractions for point-based differentiable rendering. Taichi [ 17 ] supports multiple backends, but the point-based rendering framework, Taichi-splatting [ 5 ], requires CUDA-specific modules. We propose XPR, a point-based differentiable rendering frame-work designed to address both challenges. XPR has three design goals: (1) Easy to program for quick prototyping: XPR should offer an intuitive high-level programming interface. A programmer implements a new point-based rendering method by specifying only the method-specific logic in a few lines of code without modi-fying the rendering pipeline, its parallelization, or its data layout. 

(2) General: XPR should be sufficiently expressive to cover a large design space of point-based differentiable rendering. Specifically, we design XPR for any differentiable rendering pipeline that can be expressed as a mapping between point-based primitives and pixels in an image plane. (3) Portable and Interoperable: XPR should express the pipeline’s computation and parallelism in a high-level hardware-agnostic form that (i) a cross-platform compiler can lower to diverse accelerators and (ii) is compatible with diverse IRs like StableHLO [ 37 ], and ATen IR (torch.fx) [ 46 ] which integrate with ML frameworks. XPR is designed based on three key ideas: (1) A four-function interface that separates method-specific logic. XPR abstracts the rendering pipeline as a function that computes a single prim-itive’s contribution at a single pixel. This function is exposed to the programmer as four interface functions— project , tile_cull ,

pixel_info , and evaluate —that capture only the method-specific logic, while decoupling primitive filtering, tile assignment, depth sorting, alpha-blending, parallelization, and differentiation. (2) Single-element functions. Each interface function operates on a single primitive, a single pixel, or a single primitive–pixel pair. The programmer never writes loops over primitives, pixels, or their 

Table 2. Generality of XPR across point-based rendering methods. Each row shows a variation class expressible in XPR, and represen-tative works.           

> Variation class Published point-based rendering methods expressible in XPR
> Primitive type 3DGS [ 24 ], 2DGS [ 18 ], GES [ 15 ], TriangleSplatting [ 16 ], LinPrim [ 52 ], EVER [ 31 ], 3D Superquadric Splatting [30] Pixel color/feature computation 3DGS [24], 2DGS [18], GES [15], 3DGUT [54], LinPrim [52], EVER [31] Rendered feature 3DGS [24], LangSplat [43], LangSplatv2 [28], SplaTAM [22], GS-SLAM [55], Surfels [9] Primitive-tile selection StopThePop [44], FlashGS [11] Stored representation ContraGS [10], C3DGS [42]

intersections—XPR automatically generates the iteration over all primitives and pixels and filters out non-contributing primitive-pixel pairs through tiling and culling. (3) Portable decomposi-tion of the rendering pipeline. XPR decomposes the rendering pipeline into a sequence of modules , each with a single, explicit di-mension of parallelism—e.g., across primitives, across tiles, or across pixels. Each module’s computation is expressed using building blocks available across all accelerators: data parallel element-wise operations over a batch of primitives, pixels, tiles, or primitive-tile pairs, and standard parallel primitives such as prefix-sum, sort, scat-ter, and gather. This exposes the renderer’s parallelism in a form that a cross-platform compiler can lower to threads and warps on GPUs, or to batched operations on TPUs, from a single implementation. Critically, all tensor dimensions and loop trip counts are fixed at compile time—converting the pipeline’s inherently dynamic struc-ture, such as variable per-tile primitive lists and data-dependent blend loops, into statically shaped operations that satisfy the con-straints of many ML accelerators and IRs. To demonstrate the simplicity and flexibility of XPR, we im-plement and evaluate point-based rendering methods, including 3DGS [ 24 ], LinPrim [ 52 ], and 3DGUT [ 54 ] (see §6.1). We show that the implementation requires up to an order-of-magnitude fewer lines of code than its CUDA counterpart. Beyond these, Table 2 (non-comprehensively) lists more recent works that can be easily implemented with XPR. We validate rendering quality by evalu-ating PSNR, SSIM, and LPIPS against ground-truth images, and find that XPR matches the corresponding CUDA baselines across these metrics. When implemented with the JAX/XLA compiler, we demonstrate that XPR can execute on NVIDIA, AMD, and Intel GPUs, and Google TPUs. On the NVIDIA L40S GPU, XPR reaches rendering speeds that are, on average, 97.18% of those of hand-optimized CUDA baselines. This paper makes the following contributions. (1) We present XPR, the first framework that enables expressing point-based differ-entiable rendering methods using intuitive high-level programming interfaces that decouple method-specific logic from rasterization, parallelization, and hardware mapping. XPR generates high-level parallelized hardware-agnostic modules that can be mapped to diverse compute hardware. (2) We provide a Python-based im-plementation of XPR interfaced with XLA that can be used for exploration, rapid prototyping, teaching, and code reproduction on a range of hardware platforms. (3) We demonstrate ease-of-programming and generality by implementing 3DGS, 3DGUT, and 2LinPrim in 260 to 304 lines each, matching baseline rendering qual-ity on 17 scenes from three standard benchmarks, and report ren-dering speeds across four hardware targets for three methods. (4) To facilitate future research, we aim to open-source the complete codebase of XPR. 

## 2 Background 

We review the point-based rendering pipeline using 3DGS [ 24 ] as an example. Each 3DGS primitive is a 3D Gaussian density with center 𝜇 and covariance Σ = 𝑅𝑆𝑆 ⊤𝑅 ⊤ (where 𝑅 is rotation and 

𝑆 diagonal scale), given by 𝐺 (x) = exp  − 12 (x − 𝜇 )⊤ Σ−1 (x − 𝜇 ),opacity 𝑜 , and spherical harmonics for view-dependent color. The rendering pipeline has four stages. 

Preprocess. The preprocess stage computes, for each 3D Gaussian, a 2D footprint (2D mean and 2D covariance) on the image plane [ 59 ], an axis-aligned bounding box in tile coordinates, and per-primitive attributes such as the tile count, depth, and view-dependent color. We use projection to denote the transformation that, given the cam-era, maps a primitive’s 3D position and shape parameters to its screen-space footprint. Primitives outside the view, with negligible opacity, or smaller than one tile, are flagged for filtering. 

Filter. The filter stage removes primitives flagged during the pre-process step. Only visible primitives are kept for rasterization. 

Rasterizer. Each visible 3D Gaussian primitive is assigned to the tiles its bounding box intersects, and the renderer builds a depth-sorted list of contributing primitives per tile. Optimizations such as tile culling [ 44 ] further prune non-contributing primitives from each list. 

Shader. For each pixel within a tile, the renderer evaluates each 3D Gaussian primitive’s contribution color in depth order using 

𝛼 -blending [ 33 ]. We refer to the computation of a single primi-tive’s contribution at a single pixel, before 𝛼 -blending, as per-pixel evaluation . For 3D Gaussians, per-pixel evaluation computes the primitive’s response , the unnormalized Gaussian density at a query location, and weights it by the primitive’s opacity. The color of pixel (𝑥, 𝑦 ) is 𝑐 (𝑥, 𝑦 ) = Í𝑁 𝑡  

> 𝑖 =1

c𝑖 𝛼 𝑖 𝑇 𝑖 , where 𝑁 𝑡 is the number of 3D Gaussians in tile 𝑡 ’s list, c𝑖 and 𝛼 𝑖 are the color and opacity of the 

𝑖 -th primitive, and 𝑇 𝑖 = Î𝑖 −1 

> 𝑗 =1

(1 − 𝛼 𝑗 ) is the accumulated trans-mittance (the fraction of light remaining after previously blended primitives). The blend loop terminates when transmittance drops below a threshold, typically well before processing all 𝑁 𝑡 primitives. In Fig. 1, we illustrate variations of point-based rendering meth-ods using three representative examples: 3DGS uses 3D Gaussian primitives. The projection of a 3D Gaussian onto the image plane produces a 2D elliptical footprint. Per-pixel evaluation computes the response of the projected 2D Gaussian at each pixel location. 

3DGUT [54 ] uses the same 3D Gaussian primitive but projects it by transforming a set of representative 3D points to estimate the 2D footprint. Per-pixel evaluation casts a ray with direction d from the camera origin o through each pixel and computes the maximum re-sponse of the Gaussian in 3D along the ray. LinPrim [52 ] replaces the Gaussian with an octahedron. Projection maps the octahedron’s vertices onto the image plane. Per-pixel evaluation similarly casts a ray but computes the contribution from the path length through the octahedron. 

> Fig. 1. Projection and per-pixel evaluation for three representative point-based rendering methods.

## 3 XPR Overview 3.1 Key Interfaces 

XPR provides a programming interface that is designed for a pro-grammer to define how a single point-based primitive contributes to a single pixel’s color , without managing parallelization or hardware mapping. Thus, the simplest interface would be a single function 

evaluate_full (𝑝 , 𝑝𝑥 , 𝑐𝑎𝑚 , 𝑣𝑖𝑒𝑤 ) that takes a primitive 𝑝 , a pixel location 𝑝𝑥 = (𝑥, 𝑦 ), camera parameters 𝑐𝑎𝑚 , and a camera view ,and returns the primitive’s color contribution at that pixel. Render-ing would then call evaluate_full for every primitive–pixel pair and aggregate. This is prohibitively expensive: for 𝑁 primitives and 𝑊 · 𝐻 pixels (image width 𝑊 , height 𝐻 ), rendering requires 

𝑁 · 𝑊 · 𝐻 calls (typically > 10 12 ). XPR instead exploits two forms of redundancy that are present in every point-based rendering method. First, evaluate_full is not jointly dependent on 𝑝 and 𝑝𝑥 everywhere: some sub-computations depend only on the primitive (and are therefore identical across all pixels), while others depend only on the pixel (and are iden-tical across all primitives). XPR separates these into two dedi-cated functions. project (𝑝, 𝑐𝑎𝑚, 𝑣𝑖𝑒𝑤 ) processes each primitive once. project produces tile cull data 𝑝 𝑡𝑐𝑑 and shader data 𝑝 𝑠𝑑 .The shader data—typically a projected 2D position, 2D shape pa-rameters, opacity, and view-dependent color—is reused across all pixels. pixel_info (𝑝𝑥, 𝑐𝑎𝑚, 𝑣𝑖𝑒𝑤 ) processes each pixel once and produces pixel data 𝑝𝑥 𝑑𝑎𝑡𝑎 that is reused across all primi-tives. The per-primitive-per-pixel evaluation is then expressed as 

evaluate (𝑝𝑥 𝑑𝑎𝑡𝑎 , 𝑝 𝑠𝑑 ), which is called only on the subset of primi-tive–pixel pairs that actually interact. Second, each primitive affects only a small, spatially localized subset of pixels. XPR exploits this by grouping pixels into rectangular tiles and testing primitives against tiles rather than against individual pixels. A primitive that does not overlap with a tile is skipped for every pixel in that tile. The pro-grammer expresses this tile-level test through tile_cull (𝑡 min , 𝑡 max ,

𝑝 𝑡𝑐𝑑 ) that takes the tile boundaries 𝑡 min and 𝑡 max of tile 𝑡 and 𝑝 𝑡𝑐𝑑 

as input. tile_cull returns whether primitive 𝑝 may contribute to any pixel in tile 𝑡 .Together, the four functions— project , tile_cull , pixel_info ,and evaluate —form XPR’s programming interface. The key de-sign property is that each function operates on a single element: a single primitive, a single tile–primitive pair, or a single pixel. This means the programmer never reasons about parallelism, data lay-out, or scheduling, which is handled by XPR automatically. Fig. 2 shows how the four functions are invoked within XPR’s four-stage pipeline 5 . In Preprocess 6 , project 1 runs once per primitive 3to produce 𝑝 𝑡𝑐𝑑 , 𝑝 𝑠𝑑 , the depth 𝑧 , the bounding box 𝑎𝑎𝑏𝑏 , and a vis-ibility flag 𝑣𝑖𝑠𝑖𝑏𝑙𝑒 . Filter 7 discards primitives that are not 𝑣𝑖𝑠𝑖𝑏𝑙𝑒 .The rasterizer builds, for each tile, a depth-sorted list of the primi-tives that may contribute to it: a framework-provided axis-aligned overlap test (intersect 8 ) performs a conservative bounding-box check, and the user-defined tile_cull 2 , 9 optionally refines this at tile granularity. The shader runs pixel_info 3 , 10 once per pixel, then evaluate 4 , 11 once per pixel–primitive pair over the gathered primitives in each pixel’s tile, accumulating contributions into the final pixel color via 𝛼 -blending. The programming interface of XPR decouples method-specific logic from the pipeline: swapping 3D Gaussians for octahedra, or switching from splatting-based to ray-based evaluation, requires only new implementations of the four functions; the pipeline, its parallelization, and its data structures remain unchanged. Because JAX provides automatic differentiation, no backward pass needs to be implemented—gradients flow through the four user-defined functions for free. For 3DGS, the programmer instantiates only 

project , tile_cull , and evaluate . project computes the 2D Gaussian parameters via EWA splatting [ 59 ], and evaluate com-putes the per-pixel 2D Gaussian response (Listing 1). For LinPrim, 

project stores the 𝐿 1-slab parameters of the projected octahedron, 

pixel_info is set to None , and evaluate reconstructs the pixel ray from the pixel coordinate using the +0.5 pixel-center convention before returning the Beer-Lambert opacity from the ray’s entry and exit depths through the octahedron. For 3DGUT, project ap-plies the Unscented Transform to estimate the 2D footprint from seven sigma points; pixel_info casts a ray from the camera origin through each pixel; and evaluate returns the analytic maximum Gaussian response along that ray (see §A.1.2). In all three cases, the remainder of the pipeline—filtering, rasterization, shading, and differentiation—is reused or unchanged. 

> Fig. 2. Overview of how the programmer interface functions exposed by XPR are used in the rendering pipeline.

## 3.2 Exposing Parallelism in the Renderer to the Compiler 

XPR exposes hardware-agnostic parallelism in the rendering pipeline that can be used by a cross-platform compiler (XLA [ 38 ]in our implementation) to target a diverse set of compute hard-ware. To achieve this, XPR decomposes the rendering pipeline into 

modules . Every module falls into one of two categories. Modules whose computation can be parallelized across input data elements are expressed as map_fn(f) . map_fn(f) applies a scalar function f

independently to each element of a batch, where an element may be a primitive, a pixel, a tile, a primitive–tile pair, or a primitive–pixel pair. Because invocations are independent, map_fn exposes data parallelism that the compiler maps to SIMD execution on GPUs or to batched tensor operations on TPUs. A module can also comprise standard parallelization primitives: gather , scatter , prefix-sum ,and sort . These primitives handle the steps that require inter-element communication, and optimized implementations are typ-ically available across all target hardware. A key additional con-straint is that TPUs and similar accelerators require all tensor di-mensions and loop trip counts to be known at compile time. Several pipeline stages naturally produce data-dependent output sizes: the number of visible primitives after filtering and the total number of primitive–tile intersections both vary from frame to frame. XPR handles this by fixing all such dimensions to compile-time upper bounds, padding unused entries, and replacing dynamic loops with fixed-trip-count loops that exit early. We now discuss how the rendering pipeline algorithm can be expressed as a composition of modules, and how each module is parallelized using map_fn 

or standard operations ( gather /scatter / prefix-sum /sort ). We highlight each map_fn scalar function in <red> , and indicate the elements it parallelizes over in braces ( primitives , pixels , tiles ,or intersections ): (1) Preprocess. project is applied independently to each of the 𝑁 

primitives via a single map_fn , as illustrated in Fig. 3. (2) Filter. Starting from the per-primitive visibility flags computed in Preprocess, a map_fn over the 𝑁 primitives builds a sort key from the primitive’s flag and depth, a sort brings the visible primi-tives to the front in depth order, and a gather collects them into a contiguous array of 𝑀 < 𝑁 visible primitives. 

> Fig. 3. Parallelizing the preprocess and filter stages using modules.

(3) Rasterizer. Between 𝑇 tiles of pixels and 𝑀 primitives, directly testing all 𝑀 × 𝑇 primitive–tile pairs for intersections is infeasible at scale ( 𝑀 ∼ 10 6, 𝑇 ∼ 10 5). Instead, XPR exploits the fact that each primitive intersects only a small number of tiles—bounded by its bounding box area—and enumerates only those candidate intersections in a flat array of size 𝐼 (= number of tile intersections). A map_fn over primitives counts tile intersections per primitive 3

(Fig. 4). A repeat (prefix-sum +scatter ) module expands each 4primitive index by its count 4 ; a second map_fn assigns a unique tile ID to each intersection 5 ; and a gather assembles the per-tile primitive lists 6 . The full procedure, described in section 4.2, produces a statically shaped 𝑇 × 𝑃 max matrix of primitive indices per tile. 

> Fig. 4. Modules used to parallelize the rasterizer.

(4) Shader. All pixels within a tile iterate over the same primitive list, so the per-pixel color computation is parallelized via map_fn 

over the pixels in a tile 8 (2 × 2 pixel tile shown in Fig. 5). The remaining challenge is that different tiles intersect different num-bers of primitives, so a single static loop bound applied uniformly would waste substantial computation on lightly loaded tiles. XPR addresses this with trip-count binning : tiles are sorted by primitive count and partitioned into multiple bins, each processed with a static trip count equal to its bin’s upper bound 7 . Tiles within the same bin are parallelized via a second map_fn over tiles. The full shader design is described in section 4.3. 

> Fig. 5. Parallelizing the shader using modules.

## 4 Detailed Design 4.1 Preprocess and Filter 

Preprocess. XPR expresses this stage as map_fn that uses project 

and 𝑁 primitives as arguments to expose parallelism. Each primi-tive is processed independently by hardware processing units. The operation project computes the projected footprint, bounding box, depth, a visibility flag, shader data, and tile cull data per primitive. 

Filter. The filter stage reduces the working set from 𝑁 total prim-itives to 𝑀 visible ones ( 𝑀 < 𝑁 ). Filter applies a map_fn over 𝑁 

primitives to extract the indices of visible primitives using their visibility flags. The map_fn then uses these indices and the depths to construct a sort key that is used to sort the indices of visible primitives to the front in depth-sorted order. A gather then uses the sorted indices to collect the 𝑀 visible primitives into a contigu-ous array. The per-primitive tile counts of the 𝑀 visible primitives are passed to the rasterizer. The sum of these tile counts gives the total intersection count 𝐼 , which the rasterizer uses to size its flat intersection array. 

## 4.2 Rasterizer 

The rasterizer determines, for each pixel tile, which primitives can contribute to it. Because primitives are distributed unevenly across the image, the number of contributing primitives varies from tile to tile, and the natural representation is a per-tile list of variable length. Variable-length lists, however, require dynamic memory and dynamic control flow, neither of which is well supported on accelerators that compile to static tensor shapes. XPR therefore reformulates rasterization as a sequence of modules, producing a fixed-size matrix 𝑄 of size 𝑇 × 𝑃 max in which row 𝑡 stores up to 

𝑃 max depth-sorted primitive indices for tile 𝑡 .XPR’s rasterizer receives 𝑀 primitives 𝑝 0, . . . , 𝑝 𝑀 −1 as input from the filter stage. For each primitive 𝑝 𝑖 , project returns its depth 𝑧 𝑖 , its tile-space bounding box [𝑥 min  

> 𝑖

, 𝑥 max  

> 𝑖

) × [ 𝑦 min  

> 𝑖

, 𝑦 max  

> 𝑖

),and its tile count 𝑡𝑐 𝑖 (the number of tiles its bounding box inter-sects). Fig. 6 shows the five processing stages of XPR’s rasterizer, explained below. 

Replicate ❶. XPR prefix-sums the tile counts 𝑡𝑐 𝑖 to obtain each primitive’s start offset 𝑠 𝑖 in a flat array of size 𝐼 . Each element of this flat array corresponds to a tile-primitive intersection. A map_fn 

over the 𝐼 elements writes 𝑝 𝑖 into slots 𝑠 𝑖 , . . . , 𝑠 𝑖 + 𝑡𝑐 𝑖 − 1, so that 𝑝 𝑖 

appears exactly 𝑡𝑐 𝑖 times. For example, in Fig. 6, primitive 𝑝 0 shown in blue is assigned to tiles 𝑡 0, 𝑡 1, 𝑡 3, and 𝑡 4. Assign tile ID ❷. We assign a unique tile ID for each intersection. For a replicated entry with local offset 𝑟 within 𝑝 𝑖 ’s bounding box, let 𝑤 𝑖 = 𝑥 max  

> 𝑖

− 𝑥 min  

> 𝑖

.XPR computes 𝑥 = 𝑥 min  

> 𝑖

+ ( 𝑟 mod 𝑤 𝑖 ) and 𝑦 = 𝑦 min  

> 𝑖

+ ⌊ 𝑟 /𝑤 𝑖 ⌋, then assigns tile ID 𝑡 = 𝑦𝑊 ′ + 𝑥 , where 𝑊 ′ is the tile-grid width. Cull 

❸. XPR applies the optional tile_cull function to each candidate primitive–tile pair, using the tile-cull data returned by project .Candidates that fail tile_cull or the contribution threshold retain their slot but are reassigned tile ID 𝑇 . If no tile_cull function is provided, XPR keeps all bounding-box candidates. Sort and identify bounds ❹. XPR lexicographically sorts the flat array by (tile ID, depth), placing rejected candidates at the end. For each valid tile 𝑡 𝑗 , the surviving candidates form a single contiguous segment whose primitive indices are ordered front-to-back. A map_fn over the sorted tile IDs records each segment’s start 𝑏 𝑗 and count 𝑐 𝑗 .The visual gaps in Fig. 6 mark these segment bounds; they are not empty entries in the flat array. Gather ❺. Using 𝑏 𝑗 and 𝑐 𝑗 , XPR writes 𝑄 [ 𝑗, 𝑘 ] = 𝑃 sort [𝑏 𝑗 +𝑘 ] for 0 ≤ 𝑘 < min (𝑐 𝑗 , 𝑃 max ), where 𝑃 sort 

is the sorted primitive-index array. XPR pads the remaining entries of 𝑄 [ 𝑗, :] with −1. The shader then reads 𝑄 , the counts 𝑐 𝑗 , and the per-primitive shader data, obtaining front-to-back primitive lists with bounded per-tile work and without 𝑀 × 𝑇 storage or dynamic per-tile lists. 

> Fig. 6. XPR’s rasterizer using modules over static-shape arrays.

54.3 Shader 

The shader computes per-pixel colors by blending primitive con-tributions in depth order. It receives from the rasterizer (§4.2) the 

𝑇 × 𝑃 max depth-sorted primitive-index matrix 𝑄 , and for each pixel iterates over the primitives in its tile’s row of 𝑄 , accumulating their contributions via 𝛼 -blending. Because the number of primitives per tile varies, the number of 𝛼 -blending iterations—the loop’s trip count —varies across tiles. Accelerators such as TPUs, however, re-quire statically bounded loops, and using a single worst-case trip count across all tiles would waste substantial work on lightly loaded ones. XPR addresses this with trip-count binning : tiles are grouped by primitive count, and each group is shaded with a single static trip count. 

Trip-count Binning. The idea behind trip-count binning is to replace a single global worst-case trip count with many tighter ones for different tiles. Thus, instead of using a single worst-case trip count 

𝑃 max , we define trip counts 𝑃 0, 𝑃 1, . . . , 𝑃 𝐿 −1. These 𝑃 0, 𝑃 1, . . . , 𝑃 𝐿 −1

are hyperparameters specified before rendering. XPR assigns the 

𝑇 tiles into 𝐿 bins according to their primitive counts: bin 𝑙 ∈{0, . . . , 𝐿 − 1} holds tiles with at most 𝑃 𝑙 primitives, with 𝑃 0 <

𝑃 1 < · · · < 𝑃 𝐿 −1 = 𝑃 max . Tiles in the same bin are then shaded together with a single static trip count 𝑃 𝑙 , so lightly loaded tiles avoid the worst-case cost incurred in larger bins. For each bin, a gather collects the corresponding rows of 𝑄 into a smaller fixed-size matrix with at most 𝑃 𝑙 primitive entries per tile; entries beyond a tile’s valid count are padded with sentinels and gated to zero opacity, so they do not affect the rendered color. Fig. 7 illustrates the scheme with 𝐿 = 3 bins of sizes 𝑃 0 = 1, 𝑃 1 = 2, and 𝑃 2 = 4.The batch size 𝑏 𝑙 sets how many primitive entries the shader loads and processes at each step for bin 𝑙 . The shader exposes parallelism within each bin by mapping over tiles in the bin and, inside each tile, over its pixels. For each pixel (𝑥, 𝑦 ) in a tile of bin 𝑙 , XPR iterates through that tile’s primitive list for up to 𝑃 𝑙 iterations, the bin’s static trip count.     

> Fig. 7. The shader receives 𝐿 bins. Each bin contains tiles whose primitive counts are bounded by one static trip count. The shader processes each tile’s primitive list in batches of 𝑏 𝑙 entries.

Algorithm 1 describes the process for a bin 𝑙 . For each tile 𝑡 , the shader receives as input the trip count 𝑃 𝑙 , the batch size 𝑏 𝑙 , and the list 𝑄 𝑡 of primitive indices to iterate through. The shader loops across 𝑄 𝑡 primitives for pixel tile 𝑡 with a tile size of 𝑤 × ℎ (tile width 𝑤 , height ℎ) in bin 𝑙 using map_fn over pixels in the tile. The loop is structured as a two-level loop with a fixed outer trip count. 

Algorithm 1 Processing primitives for tile 𝑡 in bin 𝑙 .

Require: Trip count 𝑃 𝑙 , batch size 𝑏 𝑙 , shader data 𝑠𝑑 , list of primi-tive indices 𝑄 𝑡 for tile 𝑡 , camera parameters 𝑐𝑎𝑚 , and camera 

𝑣𝑖𝑒𝑤 

Ensure: Rendered pixel colors for pixel tile 𝑡  

> 1:

for each pixel 𝑝𝑥 in tile 𝑡 do ⊲ parallelized via map_fn  

> 2:

𝑝𝑥 𝑑𝑎𝑡𝑎 = pixel_info (𝑝𝑥, 𝑐𝑎𝑚, 𝑣𝑖𝑒𝑤 ) 

> 3:

for 𝑖 = 0 to ⌊𝑃 𝑙 /𝑏 𝑙 ⌋ − 1 do ⊲ ⌊𝑃 𝑙 /𝑏 𝑙 ⌋ iterations  

> 4:

for 𝑗 = 0 to 𝑏 𝑙 − 1 do ⊲ unrolled by factor 𝑈  

> 5:

𝑘 = 𝑖 · 𝑏 𝑙 + 𝑗  

> 6:

𝑞 = 𝑄 𝑡 [𝑘 ] ⊲ get primitive index  

> 7:

𝑝 𝑠𝑑 = 𝑠𝑑 [𝑞 ] ⊲ fetch primitive shader data  

> 8:

𝛼, 𝑣𝑎𝑙𝑖𝑑, 𝑐 ← evaluate (𝑝𝑥 𝑑𝑎𝑡𝑎 , 𝑝 𝑠𝑑 ) 

> 9:

. . . ⊲ gate and accumulate color  

> 10:

end for  

> 11:

. . . ⊲ check exit conditions at batch boundary  

> 12:

end for  

> 13:

end for 

## 5 Methodology 

We implement XPR in Python, using JAX [ 6] and the XLA [ 38 ]compilation framework. 

Evaluation Platforms and Datasets. We evaluate XPR on the NVIDIA L40S GPU ( L40 ), AMD MI210 GPU ( MI210 ), Intel Max 1100 GPU ( MAX1100 ), and Google v6e TPU ( TPU ). We present an evaluation of nine real-world scenes from the Mip-NeRF360 [ 4] and Tanks&Temples [ 25 ] datasets, and eight synthetic scenes from the Blender [34] dataset. 

Baselines, Configurations, and Metrics. We evaluate three XPR implementations against the corresponding authors’ released CUDA codebases: XPR-3DGS against 3DGS [ 23 , 24 ], XPR-3DGUT 

against 3DGUT [ 36 , 54 ], and XPR-LinPrim against LinPrim [ 52 , 53 ]. We refer to these codebases as Base . We train with Base using the authors’ standard configuration, then render the same trained model with both Base and XPR. We report PSNR/SSIM/LPIPS and FPS on test views; the relative speed of XPR compared to Base 

is reported as the mean of per-scene FPS ratios. Each Base ren-derer is a heavily optimized CUDA implementation specialized for one rendering method, with hand-written forward and backward kernels; together, these baselines provide a strong comparison for XPR, which expresses all three methods in a single framework. We evaluate two configurations of XPR with different maximum intersections per tile, 𝑃 max ∈ { 4096 , 8192 } (see (3) Rasterizer in sec-tion 3.2), denoted XPR-4K and XPR-8K .

Hyperparameters. Expressing the rendering pipeline as static-shape operations is what enables XPR to execute on accelerators that lack dynamic control flow, such as TPUs. This design requires that all tensor dimensions and loop trip counts are fixed at compile time. Two parameters in particular are data-dependent and vary across views: the maximum number of visible primitives 𝑀 and the maximum sum of per-primitive tile counts 𝐼 . Four additional hyperparameters affect rendering speed: tile size 𝑤 × ℎ, unroll fac-tor 𝑈 , number of bins 𝐿 , and batch divisor 𝐷 . The tile size 𝑤 × ℎ

specifies the shape of each pixel tile, with width 𝑤 , height ℎ, and 𝑤ℎ 

pixels per tile. The number of bins 𝐿 determines how many shader trip-count bins are used to group tiles by primitive count. For bin 𝑙 ,6the batch divisor 𝐷 is used to derive the shader batch size 𝑏 𝑙 from the bin trip count 𝑃 𝑙 , with 𝑏 𝑙 = ⌊𝑃 𝑙 /𝐷 ⌋ rounded to a multiple of 𝑈 .The unroll factor 𝑈 specifies the XLA unroll factor for the inner loop over the 𝑏 𝑙 primitive entries in each shader batch. XPR’s hy-perparameter selection determines all six settings automatically, so the user does not need to configure any hyperparameters manually. The hyperparameter selection takes as input the set of views to be rendered and the trained primitive parameters, and tunes all six necessary settings that are reused for all subsequent processing of that scene on that hardware. Table 3 summarizes the XPR hyperparameter search space. 𝑃 max 

is a global accuracy/speed tuning knob shared by all three methods. 

𝐿 , 𝐷 , and 𝑈 are selected per scene by the hyperparameter selection, enabling the configuration to adapt to scene geometry without manual tuning. 

Hyperparameter Value / Search Space 

Tile size (GPU) {8 × 8, 16 × 16 }Tile size (TPU) {16 × 16 , 32 × 32 }

𝑃 max {4096 , 8192 }

Number of bins ( 𝐿 ) {2, 4, 6, 8}

Batch divisor ( 𝐷 ) {1, 2, 4, 8}

XLA unroll factor ( 𝑈 ) {1, 2, 4, 8, 16 , 32 }

Bin upper bounds (for GPU) 64 , 128 , 256 , 512 , 768 , 1024 , 2048 

Bin upper bounds (for TPU) 256 , 512 , 768 , 1024 , 1280 , 1536 , 2048 

Table 3. XPR hyperparameter search space. 𝐿 , 𝐷 , 𝑈 are selected per scene by the hyperparameter selection; batch sizes are derived as 

b𝑙 = ⌊𝑃 𝑙 /𝐷 ⌋ rounded to a multiple of 𝑈 . 𝑃 max is the tuning knob (accuracy/speed) and is identical for all methods. 

The hyperparameter selection operates in two steps. First, it renders every target view once (forward-only, no gradients) and records 𝑀 , 𝐼 , and the number of primitives each tile intersects for each view. It sets 𝑀 and 𝐼 to the observed maxima across all views. Profiling all views is necessary because any view whose values exceed the chosen bound can produce incorrect rendered images or a drop in accuracy. Since the hyperparameter selection profiles the same views it will render, the bounds are guaranteed to be sufficient without any safety margin or overestimation heuristic. Second, the hyperparameter selection searches the four performance param-eters in five sequential phases: the tile size (two candidates; each needs to be profiled since different tile sizes change the tile grid), the unroll factor 𝑈 (six candidates, benchmarked exhaustively), the number of bins 𝐿 (four candidates, benchmarked exhaustively, with a 2% noise-tolerance tie-breaker that re-benchmarks the lowest and highest tied candidates with additional runs), a search of 𝑈 

at the chosen 𝐿 (six candidates, triggered whenever 𝐿 shifts the bin layout, since a larger unrolled scan body shifts the tradeoff be-tween instruction-level parallelism and register pressure), and the batch divisor 𝐷 (up to four candidates, preferring 𝐷 =4 within noise tolerance). The per-tile counts from profiling also determine the data-aware assignment of tiles to bins, computed from worst-case cumulative counts across all views. In total, the full hyperparameter selection (profiling and search) takes only a few minutes, and this is a one-time cost that is negligible relative to the training time of the scene. 

## 6 Evaluation 6.1 Ease of Implementing Rendering Methods 

We demonstrate that XPR can express various point-based ren-dering methods with only small code changes. XPR-3DGS requires implementing project , evaluate , and tile_cull . The project 

function computes the 2D Gaussian projection parameters via EWA splatting, and evaluate computes the per-pixel 2D Gaussian re-sponse. All other framework modules are reused unchanged. We demonstrate how XPR-3DGUT and XPR-LinPrim are implemented in Appendix A.1.                  

> 1def project (p, cam, view, cfg):
> 2# Get world to camera projection matrix
> 3R_w2c = world_2_camera_projection_matrix(view)
> 4mu_c = world_2_cam(p.mu, R_w2c)
> 5mu_2D = compute_mu_2D(mu_c, cam)
> 6# EWA splatting
> 7cov_2D, det, _ = compute_cov_2D(p.s, p.q, mu_c, R_w2c, cam, cfg)
> 8con_2D = compute_conic_2D(cov_2D, det)
> 910 # Compute extent, radius, and bounding box
> 11 ex, ey = compute_extents(cov_2D, det, p.o, cfg)
> 12 r = compute_radius(ex, ey)
> 13 aabb = get_bounding_box(mu_2D, ex, ey, cfg)
> 14 15 # Compute tile count
> 16 tc = (aabb.x_max-aabb.x_min) * (aabb.y_max-aabb.y_min)
> 17 18 # Compute visibility flag and adjust tile count
> 19 visible = (det>0) & (p.o>=cfg.alpha_thresh) & (r > 0) & (tc>0) &

↩→ (mu_c[2]>cfg.z_near)      

> 20 tc *= float(visible)
> 21 22 # Compute view-dependent color c
> 23 c = compute_color(p.mu, p.sh, view)
> 24 # Produce tile cull data and shader data for primitive p
> 25 p_tcd = concat(con_2D[0,0], con_2D[0,1], con_2D[1,1], p.o, mu_2D,

↩→ visible)  

> 26 p_sd = concat(mu_2D[0], mu_2D[1], -0.5*con_2D[0,0], -con_2D[0,1],

↩→ -0.5*con_2D[1,1], p.o, c)  

> 27 28 return ProjectResult (depth=mu_c[2], visible=visible,

↩→ tile_cull_data=p_tcd, shader_data=p_sd, aabb=aabb, 

↩→ tile_count=tc)    

> 29 30 def tile_cull (tile_min, tile_max, p_tcd, cfg):
> 31 conic, o, mu_2D, visible = unpack(p_tcd)
> 32 mu_outside = (mu_2D[0] < tile_min[0]) | (mu_2D[0] > tile_max[0]) |

↩→ (mu_2D[1] < tile_min[1]) | (mu_2D[1] > tile_max[1])  

> 33 34 # Compute location of the 2D point inside the tile that maximizes the

↩→ response  

> 35 point_2D = compute_point_with_max_response(tile_min, tile_max, conic,

↩→ mu_outside)      

> 36 37 # Compute max contribution power of 2D Gaussian
> 38 dx = mu_2D[0]-point_2D[0]
> 39 dy = mu_2D[1]-point_2D[1]
> 40 power = 0.5*(dx*dx*conic[0] + 2*dx*dy*conic[1] + dy*dy*conic[2])
> 41 42 # Keep tile iff max contribution to tile satisfies: o * exp(-power) >=

↩→ cfg.alpha_thresh                

> 43 tile_visible = (power <= log(o / cfg.alpha_thresh)) & visible
> 44 return tile_visible
> 45 46 def evaluate (px_data, p_sd):
> 47 mu_2D, conic, o, c = unpack(p_sd)
> 48 dx = px_data[0] - mu_2D[0]
> 49 dy = px_data[1] - mu_2D[1]
> 50 power = dx * dx * conic[0] + dx * dy * conic[1] + dy * dy * conic[2]
> 51 alpha = o * exp(power)
> 52 valid = power<=0.
> 53 return EvaluateResult (alpha=alpha, valid=valid, color=c)
> 54 55 XPR_3DGS =MethodSpec (project= project , tile_cull= tile_cull ,

↩→ pixel_info=None, evaluate= evaluate )

Listing 1. Implementation of XPR-3DGS .

Table 4 reports the lines of code required to implement these methods in XPR compared to Base . We count non-blank lines, excluding comments, docstrings, and import/#include directives. For XPR, we count lines across programmer interface functions and any helper functions they call. Base (non-shared vs 3DGS) shows the number of new lines of code added over the 3DGS codebase. 7Each XPR implementation requires only 260 to 304 lines of Python, significantly fewer than Base .             

> 3DGS LinPrim 3DGUT
> XPR (Python) 260 304 297
> Base (total) 2,153 2,516 4,915
> Base (non-shared vs 3DGS) —862 4,122

Table 4. Lines of code comparison of XPR with the corresponding 

Base implementations for different point-based rendering methods. 

## 6.2 Quality and Performance of XPR 

To verify that XPR’s implementations are equivalent to the Base 

implementations, we evaluate rendering quality on test views of the scenes from the three datasets in terms of PSNR/SSIM/LPIPS and rendering speed in terms of FPS averaged across all scenes in a dataset on an L40 (Table 5). We report SSIM/LPIPS measurements in Appendix A.2 since XPR matches Base .Dataset 360 T&T Synthetic                                                                    

> Method Metric Base XPR-4K XPR-8K Base XPR-4K XPR-8K Base XPR-4K XPR-8K 3DGS PSNR 29.11 29.11 29.11 23.71 23.64 23.71 33.45 33.41 33.45 FPS 100.81 95.16 92.02 130.16 124.59 106.76 311.71 232.68 213.96
> 3DGUT PSNR 28.92 28.86 28.86 23.77 23.65 23.72 33.47 33.47 33.47 FPS 105.66 71.55 69.69 97.74 78.37 68.41 349.80 190.28 179.51
> LinPrim PSNR 27.94 27.93 27.94 22.22 22.22 22.22 33.20 32.62 33.20 FPS 66.31 71.39 69.73 89.06 99.17 102.09 73.10 116.42 99.28

Table 5. Comparison of average PSNR and FPS of XPR with Base 

implementations for different point-based rendering methods and datasets on an L40 .

First, we observe that XPR-8K matches Base-3DGS and 

Base-LinPrim on all three datasets in PSNR, and stays within 

0.06 PSNR compared to Base-3DGUT , which we attribute to nu-merical noise. Second, lowering 𝑃 max to 4096 leads to higher frame rates in most configurations because this reduces the shader’s 𝛼 -blending iterations. XPR-4K (the default setting) averages 1.08 × the frame rate of XPR-8K (up to 1.38 ×). A small quality loss, however, is incurred since some primitives’ contributions may be skipped: 

XPR-4K remains within 0.06 PSNR of Base on 360 and 0.12 PSNR on T&T , and within 0.58 PSNR on Synthetic , where the larger gap is attributed to LinPrim’s ray-intersection evaluation being more sensitive. Third, the frame rates for XPR-3DGS in comparison to 

Base are 0.95 × on 360 , 0.96 × on T&T , and 0.76 × on Synthetic (for 

𝑃 max = 4096 ). JAX incurs a small kernel launch overhead that is more significant when frame rates are very high, as in Synthetic .XPR’s overheads are higher on 3DGUT (69 .52 % of Base on average) because 3DGUT ’s pixel evaluation depends on per-pixel world-space ray directions, implemented via pixel_info in XPR (Listing 3). 

Base fuses this per-pixel computation into blending, making it faster. 3DGS avoids this cost because its evaluation depends only on 2D pixel coordinates. XPR-LinPrim outperforms Base because we implement a more aggressive tile_cull strategy that prunes more primitive–tile intersections, reducing computation (Listing 5). This demonstrates that XPR’s high-level interface enables rapid prototyping of efficient rendering strategies. Thus, we demonstrate that XPR enables prototyping and ex-ploration of a wide range of point-based differentiable rendering methods using high-level abstractions and fewer lines of code, while achieving per-scene frame rates that are, on average, within 

2.82% of those of hand-written CUDA implementations, and up to 

208 .18% faster. 

Demonstrating XPR’s portability across platforms. Table 6 reports the average FPS of XPR-4K , directly compiled from the same Python codebase via XLA without any platform-specific modifi-cations, for all three datasets on four hardware targets. To our knowledge, XPR is the first point-based differentiable renderer that can execute on all four platforms and provides the only available implementation on Intel data center GPUs and Google TPUs. Abso-lute frame rates vary across platforms, reflecting two factors: the maturity of XLA’s backend for each target and the available FP32 compute throughput. On the TPU, single-chip performance (Table 6) is lower than on GPUs, but each stage of XPR’s pipeline is paral-lelizable across chips to scale performance in multi-chip systems.                                                

> 3DGS 3DGUT LinPrim
> Platform 360 T&T Syn. 360 T&T Syn. 360 T&T Syn. MI210 29.60 40.67 67.16 23.71 33.98 73.14 22.91 34.39 33.79
> L40 95.16 124.59 232.68 71.55 78.37 190.28 71.39 99.17 116.42
> MAX1100 9.95 12.83 18.59 6.07 6.92 11.00 9.36 15.33 16.68
> TPU 1.89 2.22 6.19 1.52 1.19 6.92 1.70 3.00 3.02

Table 6. Average rendering FPS of XPR-4K on various platforms. 

## 7 Discussion 

The primary goal of XPR is to enable rapid prototyping, exploration, and code reproduction across a wide range of platforms. XPR is designed for compatibility with existing cross-platform compilers and ML IRs. Thus, compiler optimizations and hardware-specific performance tuning are outside of the scope of this work. A key resulting limitation is that rendering efficiency on any given plat-form is bounded by the maturity of the compiler backend for that target. XPR currently uses JAX/XLA [ 38 ] with HLO as the inter-mediate representation, and supports any hardware target with an XLA backend, including NVIDIA, AMD, and Intel GPUs, TPUs, Trainium, and CPUs (we present evaluation on a representative sub-set). However, any compiler stack that can efficiently lower XPR’s map_fn, scatter, gather, and sort operations is a viable backend—for example, TorchDynamo with ATen IR [ 1]. In terms of generality and expressiveness, XPR is powerful enough to express a wide range of point-based rendering techniques (examples non-comprehensively listed in Table 2). Techniques that involve ray marching or ray trac-ing (e.g., NeRFs, physically-based rendering) cannot be expressed with XPR. 

## 8 Conclusion 

We presented XPR, an extensible point-based differentiable ren-dering framework that separates method-specific logic from the shared rendering pipeline. XPR lowers the barrier to exploring new point-based rendering techniques, learning/teaching differen-tiable rendering, and using a wider range of hardware platforms for differentiable rendering. Integration with existing ML compiler stacks also makes differentiable rendering easier to incorporate as a component within larger optimization and learning systems. 8References 

[1] Jason Ansel, Edward Yang, Horace He, Natalia Gimelshein, Animesh Jain, Michael Voznesensky, Bin Bao, Peter Bell, David Berard, Evgeni Burovski, et al . 2024. PyTorch 2: Faster Machine Learning Through Dynamic Python Bytecode Trans-formation and Graph Compilation. In Proceedings of the 29th ACM international conference on architectural support for programming languages and operating systems, volume 2 . 929–947. [2] AWS. 2026. Neuron Graph Compiler. AWS Neuron Documentation. https: //awsdocs-neuron.readthedocs-hosted.com/en/latest/compiler/index.html ac-cessed: May 7, 2026. [3] AWS. 2026. PyTorch NeuronX Tracing API for Inference. AWS Neuron Documentation. https://awsdocs-neuron.readthedocs-hosted.com/en/latest/ frameworks/torch/torch-neuronx/api-reference-guide/inference/api-torch-neuronx-trace.html accessed: May 7, 2026. [4] Jonathan T Barron, Ben Mildenhall, Dor Verbin, Pratul P Srinivasan, and Pe-ter Hedman. 2022. Mip-NeRF 360: Unbounded Anti-Aliased Neural Radiance Fields. In Proceedings of the IEEE/CVF conference on computer vision and pattern recognition . 5470–5479. [5] Oliver Batchelor. 2023. Taichi Splatting. https://github.com/uc-vision/taichi-splatting. accessed: May 7, 2026. [6] James Bradbury, Roy Frostig, Peter Hawkins, Matthew James Johnson, Yash Katariya, Chris Leary, Dougal Maclaurin, George Necula, Adam Paszke, Jake VanderPlas, Skye Wanderman-Milne, and Qiao Zhang. 2018. JAX: composable transformations of Python+NumPy programs . http://github.com/jax-ml/jax [7] Arthur Brussee. 2024. Brush: 3D Reconstruction for All. https://github.com/ ArthurBrussee/brush accessed: May 7, 2026. [8] Yuanhao Cai, Yixun Liang, Jiahao Wang, Angtian Wang, Yulun Zhang, Xiaokang Yang, Zongwei Zhou, and Alan Yuille. 2024. Radiative Gaussian Splatting for Efficient X-Ray Novel View Synthesis. In European Conference on Computer Vision .Springer, 283–299. [9] Pinxuan Dai, Jiamin Xu, Wenxiang Xie, Xinguo Liu, Huamin Wang, and Weiwei Xu. 2024. High-quality Surface Reconstruction using Gaussian Surfels. In ACM SIGGRAPH 2024 conference papers . 1–11. [10] Sankeerth Durvasula, Sharanshangar Muhunthan, Zain Moustafa, Richard Chen, Ruofan Liang, Yushi Guan, Nilesh Ahuja, Nilesh Jain, Selvakumar Panneer, and Nandita Vijaykumar. 2025. ContraGS: Codebook-Condensed and Trainable Gauss-ian Splatting for Fast, Memory-Efficient Reconstruction. In Proceedings of the IEEE/CVF International Conference on Computer Vision . 28935–28945. [11] Guofeng Feng, Siyan Chen, Rong Fu, Zimu Liao, Yi Wang, Tao Liu, Boni Hu, Linning Xu, Zhilin Pei, Hengjie Li, et al . 2025. FlashGS: Efficient 3D Gaussian Splatting for Large-scale and High-resolution Rendering. In Proceedings of the Computer Vision and Pattern Recognition Conference . 26652–26662. [12] Clement Fuji Tsang, Maria Shugrina, Jean Francois Lafleche, Or Perel, Charles Loop, Towaki Takikawa, Vismay Modi, Alexander Zook, Jiehan Wang, Wenzheng Chen, Tianchang Shen, Jun Gao, Krishna Murthy Jatavallabhula, Edward Smith, Artem Rozantsev, Sanja Fidler, Gavriel State, Jason Gorski, Tommy Xiang, Jianing Li, Michael Li, and Rev Lebaredian. 2024. Kaolin: A PyTorch Library for Accelerating 3D Deep Learning Research . https://github.com/NVIDIAGameWorks/kaolin [13] Ruiqi Gao, Aleksander Holynski, Philipp Henzler, Arthur Brussee, Ricardo Mar-tin Brualla, Pratul Srinivasan, Jonathan Barron, and Ben Poole. 2024. CAT3D: Create Anything in 3D with Multi-View Diffusion Models. Advances in Neural Information Processing Systems 37 (2024), 75468–75494. [14] Fredrik Gustafsson and Gustaf Hendeby. 2011. Some Relations Between Extended and Unscented Kalman Filters. IEEE Transactions on Signal Processing 60, 2 (2011), 545–555. [15] Abdullah Hamdi, Luke Melas-Kyriazi, Jinjie Mai, Guocheng Qian, Ruoshi Liu, Carl Vondrick, Bernard Ghanem, and Andrea Vedaldi. 2024. GES : Generalized Exponential Splatting for Efficient Radiance Field Rendering. In Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition . 19812–19822. [16] Jan Held, Renaud Vandeghen, Adrien Deliege, Abdullah Hamdi, Daniel Rebain, Silvio Giancola, Anthony Cioppa, Andrea Vedaldi, Bernard Ghanem, Andrea Tagliasacchi, et al . 2026. Triangle Splatting for Real-Time Radiance Field Render-ing. In Thirteenth International Conference on 3D Vision .[17] Yuanming Hu, Tzu-Mao Li, Luke Anderson, Jonathan Ragan-Kelley, and Frédo Durand. 2019. Taichi: A Language for High-Performance Computation on Spa-tially Sparse Data Structures. ACM Transactions on Graphics (TOG) 38, 6 (2019), 1–16. [18] Binbin Huang, Zehao Yu, Anpei Chen, Andreas Geiger, and Shenghua Gao. 2024. 2D Gaussian Splatting for Geometrically Accurate Radiance Fields. In ACM SIGGRAPH 2024 conference papers . 1–11. [19] Tianyu Huang, Wangguandong Zheng, Tengfei Wang, Yuhao Liu, Zhenwei Wang, Junta Wu, Jie Jiang, Hui Li, Rynson Lau, Wangmeng Zuo, et al . 2025. Voyager: Long-Range and World-Consistent Video Diffusion for Explorable 3D Scene Generation. ACM Transactions on Graphics (TOG) 44, 6 (2025), 1–15. [20] Wenzel Jakob, Sébastien Speierer, Nicolas Roussel, Merlin Nimier-David, Delio Vicini, Tizian Zeltner, Baptiste Nicolet, Miguel Crespo, Vincent Leroy, and Ziyi Zhang. 2022. Mitsuba 3 renderer . https://mitsuba-renderer.org. [21] Norman P Jouppi, Cliff Young, Nishant Patil, David Patterson, Gaurav Agrawal, Raminder Bajwa, Sarah Bates, Suresh Bhatia, Nan Boden, Al Borchers, et al . 2017. In-Datacenter Performance Analysis of a Tensor Processing Unit. In Proceedings of the 44th annual international symposium on computer architecture . 1–12. [22] Nikhil Keetha, Jay Karhade, Krishna Murthy Jatavallabhula, Gengshan Yang, Se-bastian Scherer, Deva Ramanan, and Jonathon Luiten. 2024. SplaTAM: Splat Track & Map 3D Gaussians for Dense RGB-D SLAM. In Proceedings of the IEEE/CVF conference on computer vision and pattern recognition . 21357–21366. [23] Bernhard Kerbl, Georgios Kopanas, Thomas Leimkühler, and George Drettakis. 2023. 3D Gaussian Splatting for Real-Time Radiance Field Rendering. https: //github.com/graphdeco-inria/gaussian-splatting. accessed: May 7, 2026. [24] Bernhard Kerbl, Georgios Kopanas, Thomas Leimkühler, George Drettakis, et al .2023. 3D Gaussian Splatting for Real-Time Radiance Field Rendering. ACM Trans. Graph. 42, 4 (2023). [25] Arno Knapitsch, Jaesik Park, Qian-Yi Zhou, and Vladlen Koltun. 2017. Tanks and Temples: Benchmarking Large-Scale Scene Reconstruction. ACM Transactions on Graphics (ToG) 36, 4 (2017), 1–13. [26] George Kopanas. 2024. Slang.D Gaussian Splatting Rasterizer. https://github. com/google/slang-gaussian-rasterization. accessed: May 7, 2026. [27] Samuli Laine, Janne Hellsten, Tero Karras, Yeongho Seol, Jaakko Lehtinen, and Timo Aila. 2020. Modular Primitives for High-Performance Differentiable Ren-dering. ACM Transactions on Graphics (ToG) 39, 6 (2020), 1–14. [28] Wanhua Li, Yujie Zhao, Minghan Qin, Yang Liu, Yuanhao Cai, Chuang Gan, and Hanspeter Pfister. 2025. LangSplatV2: High-dimensional 3D Language Gaussian Splatting with 450+ FPS. Advances in Neural Information Processing Systems 38 (2025). [29] Guanxing Lu, Shiyi Zhang, Ziwei Wang, Changliu Liu, Jiwen Lu, and Yansong Tang. 2024. ManiGaussian: Dynamic Gaussian Splatting for Multi-task Robotic Manipulation. In European Conference on Computer Vision . Springer, 349–366. [30] Daniel MacSwayne, Ales Leonardis, and Jianbo Jiao. 2026. 3D Superquadric Splatting. In Proceedings of the IEEE/CVF Winter Conference on Applications of Computer Vision . 5154–5163. [31] Alexander Mai, Peter Hedman, George Kopanas, Dor Verbin, David Futschik, Qiangeng Xu, Falko Kuester, Jonathan T Barron, and Yinda Zhang. 2025. EVER: Exact Volumetric Ellipsoid Rendering for Real-time View Synthesis. In Proceedings of the IEEE/CVF International Conference on Computer Vision . 4930–4939. [32] Hidenobu Matsuki, Riku Murai, Paul HJ Kelly, and Andrew J Davison. 2024. Gaussian Splatting SLAM. In Proceedings of the IEEE/CVF conference on computer vision and pattern recognition . 18039–18048. [33] Nelson Max. 1995. Optical Models for Direct Volume Rendering. IEEE Transactions on Visualization and Computer Graphics 1, 2 (1995), 99–108. [34] Ben Mildenhall, Pratul P Srinivasan, Matthew Tancik, Jonathan T Barron, Ravi Ramamoorthi, and Ren Ng. 2021. NeRF: representing scenes as neural radiance fields for view synthesis. Commun. ACM 65, 1 (2021), 99–106. [35] NVIDIA. 2025. Vulkan Gaussian Splatting. https://github.com/nvpro-samples/ vk_gaussian_splatting accessed: May 7, 2026. [36] NVIDIA Spatial Intelligence Lab. 2025. Official Implementation of 3D Gaussian Unscented Transform. https://github.com/nv-tlabs/3dgrut. accessed: May 7, 2026. [37] OpenXLA Project. 2024. StableHLO. OpenXLA Documentation. https://openxla. org/stablehlo accessed: May 7, 2026. [38] OpenXLA Project. 2024. XLA: Accelerated Linear Algebra. https://openxla.org/ xla accessed: May 7, 2026. [39] Jaesung Park. 2024. vkgs: Vulkan-based Gaussian Splatting Viewer. https: //github.com/jaesung-cs/vkgs [40] Jack Parker-Holder, Philip Ball, Jake Bruce, Vibhavari Dasagi, Kristian Hol-sheimer, Christos Kaplanis, Alexandre Moufarek, Guy Scully, Jeremy Shar, Jimmy Shi, Stephen Spencer, Jessica Yung, Michael Dennis, Sultan Kenjeyev, Shangbang Long, Vlad Mnih, Harris Chan, Maxime Gazeau, Bonnie Li, Fabio Pardo, Luyu Wang, Lei Zhang, Frederic Besse, Tim Harley, Anna Mitenkova, Jane Wang, Jeff Clune, Demis Hassabis, Raia Hadsell, Adrian Bolton, Satinder Singh, and Tim Rocktäschel. 2024. Genie 2: A Large-Scale Foundation World Model. (2024). https://deepmind.google/discover/blog/genie-2-a-large-scale-foundation-world-model/ [41] Ben Poole, Ajay Jain, Jonathan T Barron, and Ben Mildenhall. 2022. DreamFusion: Text-to-3D using 2D Diffusion. arXiv preprint arXiv:2209.14988 (2022). [42] Jiating Qian, Yiming Yan, Fengjiao Gao, Baoyu Ge, Maosheng Wei, Boyi Shang-guan, and Guangjun He. 2025. C3DGS: Compressing 3D Gaussian Model for Surface Reconstruction of Large-Scale Scenes Based on Multiview UAV Images. 

IEEE Journal of Selected Topics in Applied Earth Observations and Remote Sensing 

18 (2025), 4396–4409. [43] Minghan Qin, Wanhua Li, Jiawei Zhou, Haoqian Wang, and Hanspeter Pfister. 2024. LangSplat: 3D Language Gaussian Splatting. In Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition . 20051–20060. [44] Lukas Radl, Michael Steiner, Mathias Parger, Alexander Weinrauch, Bernhard Kerbl, and Markus Steinberger. 2024. StopThePop: Sorted Gaussian Splatting for View-Consistent Real-time Rendering. ACM Transactions on Graphics (TOG) 43, 4 (2024), 1–17. 

9[45] Nikhila Ravi, Jeremy Reizenstein, David Novotny, Taylor Gordon, Wan-Yen Lo, Justin Johnson, and Georgia Gkioxari. 2020. Accelerating 3D Deep Learning with PyTorch3D. arXiv:2007.08501 (2020). [46] James Reed, Zachary DeVito, Horace He, Ansley Ussery, and Jason Ansel. 2022. torch.fx: Practical Program Capture and Transformation for Deep Learning in Python. Proceedings of Machine Learning and Systems 4 (2022), 638–651. [47] shg8. 2024. 3DGS.cpp. https://github.com/shg8/3DGS.cpp accessed: May 7, 2026. [48] Yusuke Takimoto, Hiroyuki Sato, Hikari Takehara, Keishiro Uragaki, Takehiro Tawara, Xiao Liang, Kentaro Oku, Wataru Kishimoto, and Bo Zheng. 2022. Dressi: A Hardware-Agnostic Differentiable Renderer with Reactive Shader Packing and Soft Rasterization. In Computer Graphics Forum , Vol. 41. Wiley Online Library, 13–27. [49] Jiaxiang Tang, Jiawei Ren, Hang Zhou, Ziwei Liu, and Gang Zeng. 2024. Dream-Gaussian: Generative Gaussian Splatting for Efficient 3D Content Creation. In 

International Conference on Learning Representations .[50] HunyuanWorld Team, Zhenwei Wang, Yuhao Liu, Junta Wu, Zixiao Gu, Haoyuan Wang, Xuhui Zuo, Tianyu Huang, Wenhuan Li, Sheng Zhang, et al . 2025. Hun-yuanWorld 1.0: Generating Immersive, Explorable, and Interactive 3D Worlds from Words or Pixels. arXiv preprint arXiv:2507.21809 (2025). [51] Piero Toffanin. 2024. OpenSplat. https://github.com/pierotofy/opensplat ac-cessed: May 7, 2026. [52] Nicolas von Lützow and Matthias Nießner. 2025. LinPrim: Linear Primitives for Differentiable Volumetric Rendering. In The Thirty-ninth Annual Conference on Neural Information Processing Systems .[53] Nicolas von Lützow and Matthias Nießner. 2025. LinPrim: Linear Primitives for Differentiable Volumetric Rendering. https://github.com/nicolasvonluetzow/ linear-splatting. accessed: May 7, 2026. [54] Qi Wu, Janick Martinez Esturo, Ashkan Mirzaei, Nicolas Moenne-Loccoz, and Zan Gojcic. 2025. 3DGUT: Enabling Distorted Cameras and Secondary Rays in Gaussian Splatting. In Proceedings of the Computer Vision and Pattern Recognition Conference . 26036–26046. [55] Chi Yan, Delin Qu, Dan Xu, Bin Zhao, Zhigang Wang, Dong Wang, and Xuelong Li. 2024. GS-SLAM: Dense Visual SLAM with 3D Gaussian Splatting. In Proceedings of the IEEE/CVF conference on computer vision and pattern recognition . 19595–19604. [56] Vickie Ye, Ruilong Li, Justin Kerr, Matias Turkulainen, Brent Yi, Zhuoyang Pan, Otto Seiskari, Jianbo Ye, Jeffrey Hu, Matthew Tancik, et al . 2025. gsplat: An Open-Source Library for Gaussian Splatting. Journal of Machine Learning Research 26, 34 (2025), 1–17. [57] Zehao Yu, Anpei Chen, Binbin Huang, Torsten Sattler, and Andreas Geiger. 2024. Mip-Splatting: Alias-free 3D Gaussian Splatting. In Proceedings of the IEEE/CVF conference on computer vision and pattern recognition . 19447–19456. [58] Baowen Zhang, Chuan Fang, Rakesh Shrestha, Yixun Liang, Xiao-Xiao Long, and Ping Tan. 2026. RaDe-GS: Rasterizing Depth in Gaussian Splatting. ACM Transactions on Graphics 45, 2 (2026), 1–14. [59] Matthias Zwicker, Hanspeter Pfister, Jeroen Van Baar, and Markus Gross. 2002. EWA Splatting. IEEE Transactions on Visualization & Computer Graphics 8, 03 (2002), 223–238. 

## A Evaluation Details A.1 Ease of Implementing Rendering Methods 

We demonstrate that XPR can express various point-based ren-dering methods with only small code changes. XPR’s high-level programming interface separates method-specific logic from the shared rendering pipeline. XPR exposes two main data structures: (1) The PrimitiveParams data structure contains data fields that define the shape and density of the primitive. PrimitiveParams 

consists of required attributes for a primitive such as a 3D position 

𝜇 , opacity 𝑜 , and spherical harmonic coefficients 𝑠ℎ . The struc-ture can be extended with custom data fields. (2) The MethodSpec 

data structure consists of the four functions— project , tile_cull ,

pixel_info , and evaluate —that form XPR’s programming inter-face. MethodSpec specifies the rendering method, i.e., how the prim-itive is processed and rendered. Thus, XPR allows the programmer to build a new rendering method by providing the data fields in 

PrimitiveParams and implementing the four functions that are used to initialize the rendering method with MethodSpec .

A.1.1 Implementation of XPR-3DGS. Each 3DGS [24 ] primitive is a 3D Gaussian density with center 𝜇 , covariance Σ = 𝑅𝑆𝑆 𝑇 𝑅 𝑇 

(where 𝑅 is a rotation matrix and 𝑆 a diagonal scaling matrix), opac-ity 𝑜 , and spherical harmonic coefficients 𝑠ℎ for view-dependent color. In Listing 2, we show the data structure PrimitiveParams 

of XPR-3DGS .                  

> 1class PrimitiveParams :
> 2mu : Array # (3,) mean in world space
> 3s: Array # (3,) scales
> 4q: Array # (4,) unit quaternion for rotation
> 5sh : Array # (C,3) spherical harmonic coefficients
> 6o: Array # (1,) opacity

Listing 2. XPR-3DGS : PrimitiveParams .

XPR-3DGS implements project , evaluate , and tile_cull (see Listing 1). XPR-3DGS sets pixel_info =None , since per-pixel eval-uation depends only on the 2D pixel coordinates. The project 

function computes the projection of a 3D Gaussian onto the im-age plane via EWA splatting [ 59 ], yielding a 2D Gaussian density with mean 𝜇 2𝐷 and covariance Σ2𝐷 given by 𝐺 2𝐷 (x) = exp  − 12 (x −

𝜇 2𝐷 )⊤ Σ−12𝐷 (x − 𝜇 2𝐷 ) where Σ−12𝐷 is the inverse of the projected co-variance, referred to as the conic matrix . The project function then computes the bounding box 𝑎𝑎𝑏𝑏 , the tile count 𝑡𝑐 , a visibility flag 

𝑣𝑖𝑠𝑖𝑏𝑙𝑒 , and produces the tile cull data 𝑝 𝑡𝑐𝑑 and shader data 𝑝 𝑠𝑑 for primitive 𝑝 . The implementation of the tile_cull function fol-lows the approach given by StopThePop [ 44 ]: for each tile in the 3D Gaussian’s bounding box 𝑎𝑎𝑏𝑏 , tile_cull computes the 2D loca-tion x inside the tile that maximizes the contribution 𝛼 = 𝑜 ·𝐺 2𝐷 (x).Since the 2D Gaussian’s response falls off smoothly with increasing distance to 𝜇 2𝐷 , the 2D location that maximizes the contribution is either 𝜇 2𝐷 (if 𝜇 2𝐷 is located inside the tile) or a point along the edges of the tile facing 𝜇 2𝐷 . The tile-primitive pair is culled if the contribution 𝛼 falls below a threshold 𝛼 thresh = 1255 . The evaluate 

function computes the contribution 𝛼 = 𝑜 ·𝐺 2𝐷 (𝑝𝑥 ) at a pixel query location 𝑝𝑥 = (𝑥, 𝑦 ) and a validity flag 𝑣𝑎𝑙𝑖𝑑 . All other framework modules are reused unchanged. 

A.1.2 Implementation of XPR-3DGUT. 3DGUT [54 ] uses the same 3D Gaussian primitive as 3DGS but changes how primitives are projected and evaluated. Instead of linearizing the projection via the Jacobian as in EWA splatting, 3DGUT applies the Unscented Transform (UT) [ 14 ]: it selects sigma points from the 3D Gaussian, transforms each through the camera projection, and estimates the projected mean 𝜇 2𝐷 and covariance Σ2𝐷 in pixel space from the weighted projected points. The resulting 2D Gaussian is used for tile assignment and tile culling. For per-pixel evaluation, 3DGUT computes the Gaussian response in 3D along a camera ray r(𝜏 ) =

o + 𝜏 d, where o is the origin of the camera and d is the direction of the ray. The depth that maximizes the response is 

𝜏 max = (𝜇 − o)⊤ Σ−1dd⊤ Σ−1d = −o⊤ 

> c

dc

d⊤ 

> c

dc

, (1) where oc = 𝑆 −1𝑅 ⊤ (o − 𝜇 ) and dc = 𝑆 −1𝑅 ⊤d are the ray origin and direction in the canonical space of the Gaussian [ 24 , 54 ]. XPR-3DGUT 

implements all four functions of XPR’s programming interface (see Listing 3). The project function computes the UT footprint and stores the projected conic, opacity adjusted following Mip-Splatting [ 57 ], and mean in tile cull data 𝑝 𝑡𝑐𝑑 for primitive 𝑝 ; it also precomputes oc and 𝑀 = 𝑆 −1𝑅 ⊤ in shader data 𝑝 𝑠𝑑 . The tile_cull 

function follows XPR-3DGS after applying the ray-offset adjustment to the tile bounds. The pixel_info function computes the ray 10 direction d at a pixel, and evaluate uses the precomputed 𝑝 𝑠𝑑 to compute dc = 𝑀 d and the contribution 𝛼 = 𝑜 · 𝐺 (o + 𝜏 max d). All other framework modules are reused unchanged.  

> 1

def project (p, cam, view, cfg):  

> 2

# Get world to camera projection matrix  

> 3

R_w2c = world_2_camera_projection_matrix(view)  

> 4

# Construct rotation matrix R  

> 5

R = get_R(p.q)  

> 67

## Unscented Transform  

> 8

# Compute sigma points in world space  

> 9

sigma_points = compute_sigma_points(p.mu, p.s, R, cfg)  

> 10

# Project sigma points to camera space  

> 11

sigma_points_c = vectorize(world_2_cam)(sigma_points, R_w2c)  

> 12

mu_c = sigma_points_c[0]  

> 13

# Project to pixel space  

> 14

sigma_points_2D = vectorize(compute_mu_2D)(sigma_points_c, cam)  

> 15

# Estimate mu_2D and cov_2D to approximate 2D Gaussian  

> 16

mu_2D, cov_2D = approximate_2D_gaussian(sigma_points_2D, cfg)  

> 17

det_orig = determinant(cov_2D)  

> 18

# Apply low-pass filter to cov_2D (antialiasing)  

> 19

cov_2D = lowpass_filter(cov_2D)  

> 20

det = determinant(cov_2D)  

> 21

con_2D = compute_conic_2D(cov_2D, det)  

> 22 23

# Adjust opacity (Mip-Splatting)  

> 24

o = p.o * mip_splatting_2D(det_orig, det)  

> 25 26

# Compute extent, radius, and bounding box  

> 27

ex, ey = compute_extents(cov_2D, det, o, cfg)  

> 28

r = compute_radius(ex, ey)  

> 29

# Account for ray offset  

> 30

aabb = get_bounding_box(mu_2D - 0.5, ex, ey, cfg)  

> 31 32

# Compute tile count  

> 33

tc = (aabb.x_max-aabb.x_min) * (aabb.y_max-aabb.y_min)  

> 34 35

# Compute view-dependent color c  

> 36

c = compute_color(p.mu, p.sh, view)  

> 37 38

# Compute visibility flag and adjust tile count  

> 39

visible = (det>0) & (o>cfg.alpha_thresh) & (r>0) & (tc>0) & 

↩→ (mu_c[2]>cfg.z_near)  

> 40

tc *= float(visible)  

> 41 42

# Produce tile_cull_data and shader_data for primitive p  

> 43

p_tcd = concat(con_2D[0,0], con_2D[0,1], con_2D[1,1], o, mu_2D, 

↩→ visible)  

> 44

M = diag(1.0/p.s) @ R.T  

> 45

cp = -R_w2c[:3,:3].T @ R_w2c[:3,3]  

> 46

ro_canonical = (M @ (cp - p.mu)).flatten()  

> 47

p_sd = concat(ro_canonical, M.flatten(), o, c)  

> 48 49

return ProjectResult (depth=mu_c[2], visible=visible, 

↩→ tile_cull_data=p_tcd, shader_data=p_sd, aabb=aabb, 

↩→ tile_count=tc)  

> 50 51

def tile_cull (tile_min, tile_max, p_tcd, cfg):  

> 52

# Adjust tile bounds for ray offset  

> 53

tile_max[0] += 1 # tile_max_x  

> 54

tile_max[1] += 1 # tile_max_y  

> 55

# Rest of tile_cull in XPR-3DGS can be reused unchanged  

> 56

...  

> 57 58

def pixel_info (px, cam, view, cfg):  

> 59

# Camera ray direction  

> 60

dc = [(px[0] + 0.5 - cam.cx) / cam.fx, (px[1] + 0.5 - cam.cy) / 

↩→ cam.fy, 1.0]  

> 61

dc /= norm(dc)  

> 62

# Rotate to world space  

> 63

R_c2w = cam_2_world(view)[:3, :3]  

> 64

# Compute ray direction  

> 65

rd = R_c2w @ dc  

> 66

return PixelInfoResult (rd=rd)  

> 67 68

def evaluate (px_data, p_sd):  

> 69

ro_canonical, M, o, c = unpack(p_sd)  

> 70

rd = px_data[0:3]  

> 71 72

# Compute max response in canonical Gaussian space  

> 73

rd_canonical = M @ rd  

> 74

response = compute_max_response_canonical(ro_canonical,rd_canonical)  

> 75 76

alpha = o * response  

> 77

valid = (response > 0.0113)  

> 78 79

return EvaluateResult (alpha=alpha, valid=valid, color=c)  

> 80 81

XPR_3DGUT = MethodSpec (project= project , tile_cull= tile_cull ,

↩→ pixel_info= pixel_info , evaluate= evaluate )

Listing 3. Implementation of XPR-3DGUT .

A.1.3 Implementation of XPR-LinPrim. LinPrim [ 52 ] replaces 3D Gaussians with octahedra as the point-based scene representa-tion. Each octahedron is defined by a center 𝜇 , three half-extents 

(𝑠 𝑥 , 𝑠 𝑦 , 𝑠 𝑧 ) along the local coordinate axes, and a rotation quater-nion. Opposing vertices have equal distance from 𝜇 , so the primitive can be described with three scale parameters rather than six (see Listing 4).  

> 1

class PrimitiveParams : 

> 2

mu : Array # (3,) mean in world space  

> 3

s : Array # (3,) half-extents (s_x, s_y, s_z)  

> 4

q : Array # (4,) unit quaternion for rotation  

> 5

sh : Array # (C,3) spherical harmonic coefficients  

> 6

o : Array # (1,) opacity 

Listing 4. XPR-LinPrim : PrimitiveParams .

Unlike Gaussians, octahedra have a homogeneous interior den-sity: 

𝜎 (𝑜 ) = − log (1 − 0.99 · 𝑜 )

2 · min (𝑠 𝑥 , 𝑠 𝑦 , 𝑠 𝑧 ) ,

and the per-pixel opacity follows the Beer-Lambert law, 

𝛼 = 1 − exp (− 𝜎 (𝜏 exit − 𝜏 enter )) ,

where 𝜏 enter and 𝜏 exit are the entry and exit depths along the per-pixel ray. XPR-LinPrim implements project , tile_cull , and evaluate 

(see Listing 5). Thus, like XPR-3DGS , it sets pixel_info =None and 

evaluate receives the pixel coordinate directly, applying the +0.5

pixel-center offset. The project function computes the camera-space center, pixel-space center, tile-space bounding box, Euclidean depth, homogeneous density, and the linear map from the local octahedron frame to the (𝑥 px , 𝑦 px , 𝜏 ) pixel-depth frame, 

𝐴 = 𝐽 3×3𝑅 w2c ,[:3 ,:3 ] 𝑅 diag (𝑠 𝑥 , 𝑠 𝑦 , 𝑠 𝑧 ).

Here, 𝐽 3×3 is the projective Jacobian augmented with a row that normalizes by Euclidean distance from the camera, 𝑅 w2c ,[:3 ,:3 ] is the rotation of the world-to-camera transform, and 𝑅 is the rotation of the primitive. The three rows 𝑐 0, 𝑐 1, 𝑐 2 of 𝐴 −1 define the projected octahedron as 

{( 𝑝 2𝐷 , 𝜏 ) : ∥𝐴 −1

𝑝 2𝐷 − 𝜇 2𝐷 

𝜏 



∥1 ≤ 1},

where 𝜏 is relative to the center of the primitive. Equivalently, the projected octahedron is the intersection of four slab pairs with normals 𝑐 0 + 𝑐 1 + 𝑐 2, 𝑐 0 + 𝑐 1 − 𝑐 2, 𝑐 0 − 𝑐 1 + 𝑐 2, and 𝑐 0 − 𝑐 1 − 𝑐 2. The 

evaluate function intersects these slabs along the ray and converts the resulting chord length into opacity. The tile_cull function applies the per-tile upper bound derived below. The diagonal-pair terms incorporate the primitive-level opacity criterion. Thus, no separate primitive-level opacity cull is required.  

> 1

def project (p, cam, view, cfg):  

> 2

# Get world-to-camera projection matrix  

> 3

R_w2c = world_2_camera_projection_matrix(view)  

> 4

mu_c = world_2_cam(p.mu, R_w2c)  

> 5

mu_2D = compute_mu_2D(mu_c, cam)  

> 67

# Compute slab data, extent, radius, and depth  

> 8

c0, c1, c2, r, ex, ey, depth = compute_slab_data(p.s, p.q, mu_c, 

↩→ mu_2D, R_w2c, cam, cfg)  

> 910

# Compute bounding box  

> 11

aabb = get_bounding_box(mu_2D - 0.5, ex, ey, cfg)  

> 12 13

# Compute tile count  

> 14

tc = (aabb.x_max - aabb.x_min) * (aabb.y_max - aabb.y_min)  

> 15 16

# Compute density weight  

> 17

s_min = min(p.s[0], p.s[1], p.s[2])  

> 18

o = clip(p.o, 0.0, 0.999)  

> 19

sigma = -log1p(-0.99 * o) / (2.0 * s_min)  

> 20 21

# Compute visibility flag and adjust tile count  

> 22

visible = (r>0) & (tc>0) & (depth>cfg.z_near) & (s_min>0) 

11 23 tc *= float(visible)       

> 24 25 # Compute view-dependent color c
> 26 c = compute_color(p.mu, p.sh, view)
> 27 28 # Produce tile cull data and shader data for primitive p
> 29 p_tcd = concat(mu_2D, c0, c1, c2, sigma, visible)
> 30 p_sd = concat(mu_2D, c0, c1, c2, sigma, c)
> 31 32 return ProjectResult (depth=depth, visible=visible,

↩→ tile_cull_data=p_tcd, shader_data=p_sd, aabb=aabb, 

↩→ tile_count=tc)           

> 33 34 def slab_m_at_corners(mu_2D, n, x0, y0, x1, y1):
> 35 # Slab midpoint values at tile corners
> 36 INF = 1e10
> 37 EPS = 1e-10
> 38 39 nz_ok = abs(n[2]) >= EPS
> 40 nz = where(nz_ok, n[2], 1.0)
> 41 inv_nz = where(nz_ok, 1.0 / nz, 0.0)
> 42 43 corners = [(x0, y0), (x0, y1), (x1, y0), (x1, y1)]
> 44 m_corner = [-inv_nz * (n[0] * (x - mu_2D[0]) + n[1] * (y - mu_2D[1]))

↩→ for (x, y) in corners]                                

> 45 half = where(nz_ok, abs(inv_nz), INF)
> 46 47 return m_corner, half
> 48 49 def tile_cull (tile_min, tile_max, p_tcd, cfg):
> 50 mu_2D, c0, c1, c2, sigma, visible = unpack(p_tcd)
> 51 52 # Tile corners in pixel-center coordinates
> 53 x0 = tile_min[0] + 0.5
> 54 y0 = tile_min[1] + 0.5
> 55 x1 = tile_max[0] + 0.5
> 56 y1 = tile_max[1] + 0.5
> 57 58 # Four slab-pair normals
> 59 n[0] = c0 + c1 + c2
> 60 n[1] = c0 + c1 - c2
> 61 n[2] = c0 - c1 + c2
> 62 n[3] = c0 - c1 - c2
> 63 64 # Compute per-slab corner midpoints and half-widths
> 65 for iin 0..3:
> 66 m_corner[i], half[i] = slab_m_at_corners(mu_2D, n[i], x0, y0, x1, y1)
> 67 68 # Compute 16 ordered pair gaps
> 69 for iin 0..3:
> 70 for jin 0..3:
> 71 gap[i, j] = max_over_4_corners(m_corner[i] - m_corner[j]) +

↩→ half[i] + half[j]                      

> 72 73 # Bound the maximum chord length over the tile
> 74 dt_upper = max(0.0, min_over_pairs(gap))
> 75 76 # Keep tile when the opacity bound reaches the alpha threshold
> 77 required = -log(1 - cfg.alpha_thresh)
> 78 tile_visible = (sigma * dt_upper >= required) & visible
> 79 return tile_visible
> 80 81 def evaluate (px_data, p_sd):
> 82 mu_2D, c0, c1, c2, sigma, c = unpack(p_sd)
> 83 84 # Pixel-center offset
> 85 px = px_data[0:2] + 0.5
> 86 87 # Ray-octahedron slab intersection
> 88 tau_enter, tau_exit = slab_intersect(px, mu_2D, c0, c1, c2)
> 89 90 # Beer-Lambert opacity
> 91 ok = tau_enter < tau_exit
> 92 dtau = where(ok, tau_exit - tau_enter, 0.0)
> 93 alpha = where(ok, 1.0 - exp(-sigma * dtau), 0.0)
> 94 95 return EvaluateResult (alpha=alpha, valid=1.0, color=c)
> 96 97 XPR_LinPrim =MethodSpec (project= project , tile_cull= tile_cull ,

↩→ pixel_info=None, evaluate= evaluate )

Listing 5. Implementation of XPR-LinPrim .

Per-tile upper bound. Fix a tile 𝑡 and let Ω𝑡 = [𝑥 min , 𝑥 max ) × [𝑦 min , 𝑦 max ) ⊂ R2 denote the half-open rectangle in pixel space that contains the pixel centers of 𝑡 . Index the four slab pairs by 

𝑖 ∈ { 0, 1, 2, 3}, with normals 

𝑛 𝑖 = 𝜀 𝑖, 0𝑐 0 + 𝜀 𝑖, 1𝑐 1 + 𝜀 𝑖, 2𝑐 2,

where 𝜀 𝑖, 0 = +1 is fixed and 𝜀 𝑖, 1, 𝜀 𝑖, 2 ∈ {+ 1, −1}. First assume 𝑛 𝑖,𝑧 ≠ 0.Solving the inequality |𝑛 𝑖 · ( Δ𝑥, Δ𝑦, 𝜏 )| ≤ 1 for 𝜏 gives 

𝑠 lo 

𝑖 (𝑥, 𝑦 ) = 𝑚 𝑖 (𝑥, 𝑦 ) − ℎ𝑖 , 𝑠 hi 

𝑖 (𝑥, 𝑦 ) = 𝑚 𝑖 (𝑥, 𝑦 ) + ℎ𝑖 ,

where 

ℎ𝑖 = |𝑛 𝑖,𝑧 |−1 ≥ 0, 𝑚 𝑖 (𝑥, 𝑦 ) = −𝑛 −1

𝑖,𝑧 

 𝑛 𝑖,𝑥 Δ𝑥 + 𝑛 𝑖,𝑦 Δ𝑦 .

Here 𝑚 𝑖 is affine in (𝑥, 𝑦 ), Δ𝑥 = 𝑥 + 12 − 𝜇 2𝐷,𝑥 , and Δ𝑦 = 𝑦 + 12 − 𝜇 2𝐷,𝑦 .The chord length through the intersection of the four slabs at pixel 

(𝑥, 𝑦 ) is 

Δ𝜏 (𝑥, 𝑦 ) = max 



0, min 

𝑖,𝑗 ∈ { 0,1,2,3}



𝑠 hi 

𝑖 (𝑥, 𝑦 ) − 𝑠 lo 

𝑗 (𝑥, 𝑦 )



,

using the identity min 𝑖 𝑠 hi 

𝑖 − max 𝑗 𝑠 lo 

𝑗 = min 𝑖,𝑗 (𝑠 hi 

𝑖 − 𝑠 lo 

𝑗 ).We define 𝑔 𝑖 𝑗 (𝑥, 𝑦 ) = ℎ𝑖 + ℎ𝑗 + 𝑚 𝑖 (𝑥, 𝑦 ) − 𝑚 𝑗 (𝑥, 𝑦 ). Applying the weak minimax inequality and using the monotonicity of max (0, ·) 

gives 

sup 

> (𝑥,𝑦 ) ∈ Ω𝑡

Δ𝜏 (𝑥, 𝑦 ) ≤ max 0, min  

> 𝑖,𝑗 ∈{ 0,1,2,3}

"

ℎ𝑖 + ℎ𝑗 + sup 

> (𝑥,𝑦 ) ∈ Ω𝑡

 𝑚 𝑖 (𝑥, 𝑦 ) − 𝑚 𝑗 (𝑥, 𝑦 )# ! 

Since 𝑚 𝑖 − 𝑚 𝑗 is affine and continuous, its supremum over the half-open rectangle Ω𝑡 equals its maximum over the closure Ω𝑡 , and that maximum is attained at one of the four corners of Ω𝑡 . Writing 

𝐶 (Ω𝑡 ) for the set consisting of the four corners of Ω𝑡 , the per-tile upper bound used by tile_cull is 

Δ𝜏 upper (Ω𝑡 ) = max 



0, min  

> 𝑖,𝑗 ∈{ 0,1,2,3}



ℎ𝑖 + ℎ𝑗 + max    

> 𝑐 ∈𝐶 (Ω𝑡 )

 𝑚 𝑖 (𝑐 ) − 𝑚 𝑗 (𝑐 )  

(2) The minimization is over 16 ordered pairs because the chord differ-ences 𝑠 hi 

𝑖 − 𝑠 lo 

𝑗 and 𝑠 hi 

𝑗 − 𝑠 lo 

𝑖 are distinct under the inner minimum. The diagonal pairs 𝑖 = 𝑗 are included, and their entries reduce to 

𝑔 𝑖𝑖 = 2ℎ𝑖 , which is independent of the pixel and the tile. Thus, only the twelve off-diagonal pair gaps require a maximum over the four corners in 𝐶 (Ω𝑡 ). If 𝑛 𝑖,𝑧 = 0, the corresponding slab constraint is independent of 𝜏 . For this upper bound, all pair gaps involving 𝑖 are treated as +∞ , equivalently removing that slab from the outer min-imum. This can only increase Δ𝜏 upper (Ω𝑡 ), so the bound remains conservative. 

Cull predicate. A primitive is removed from 𝑡 ’s candidate list when-ever 

𝜎 Δ𝜏 upper (Ω𝑡 ) < − log (1 − 𝛼 thresh ).

The strict inequality matches the framework’s non-strict per-pixel gate 𝛼 ≥ 𝛼 thresh at the boundary, so the primitive is kept when the bound reaches the threshold. Since 𝜎 ≥ 0 by construction and 

𝛼 = 1 − exp (− 𝜎 Δ𝜏 ) is monotone non-decreasing in Δ𝜏 , the chain 

Δ𝜏 upper (Ω𝑡 ) ≥ sup 

(𝑢,𝑣 ) ∈ Ω𝑡 

Δ𝜏 (𝑢, 𝑣 ) ≥ Δ𝜏 (𝑥, 𝑦 )

implies 𝛼 (𝑥, 𝑦 ) < 𝛼 thresh for every pixel of a culled primitive-tile pair. The framework’s per-pixel alpha gate therefore masks such contributions to zero. In exact arithmetic, removing these entries is equivalent to keeping them with 𝛼 = 0: the nonzero alpha sequence is unchanged, the transmittances 𝑇 𝑖 = Î𝑘 <𝑖 (1 − 𝛼 𝑘 ) are unchanged, and the composited color is unchanged. 

Redundancy of separate per-primitive opacity culling. The cases 𝑖 = 𝑗 

in Eq. 2 reduce to 𝑔 𝑖𝑖 = 2ℎ𝑖 , which is independent of the pixel and tile. They therefore provide a global primitive-level bound on the chord length. Multiplying this bound by 𝜎 gives the primitive-level bound used by the opacity test. Since the outer minimum in Eq. 2 is taken over all ordered pairs, it cannot exceed the minimum obtained 12 from only the cases 𝑖 = 𝑗 . By the monotonicity of max (0, ·) , this gives 

Δ𝜏 upper (Ω𝑡 ) ≤ 2 min  

> 𝑖

ℎ𝑖 .

Consequently, whenever the primitive-level bound satisfies 

2𝜎 min  

> 𝑖

ℎ𝑖 < − log (1 − 𝛼 thresh ),

every tile also satisfies the cull predicate. Thus, XPR-LinPrim does not need a separate primitive-level opacity test. This matches the reference CUDA linear-splatting implementation [ 53 ], which likewise uses frustum and tile-overlap culling without adding a separate primitive-level opacity test. 

Comparison with StopThePop. Eq. 2 follows the same algorithmic pattern as the per-tile Gaussian cull in StopThePop [ 44 ]: both meth-ods compute a conservative tile-level upper bound on the maximum per-pixel opacity using the tile geometry and a small set of per-primitive constants, and both discard primitive-tile pairs whose bound is below the alpha threshold. The distinction comes from the opacity model. In LinPrim, each primitive has homogeneous density inside an 𝐿 1 polytope, so opacity is determined by the ray chord length through the primitive. The cull therefore bounds the longest possible chord over all pixels in Ω𝑡 . For a Gaussian, opacity decreases smoothly away from the mean, so StopThePop instead bounds the smallest quadratic distance from the tile to the Gauss-ian mean, equivalently the largest Gaussian response attainable within the tile. In both cases, the bound can become loose when the constraint attaining the maximum changes across Ω𝑡 , for example when different slab pairs or different closest points are active at different locations in the tile. 

## A.2 Quality of XPR                                                                       

> Dataset 360 T&T Synthetic
> Method Metric Base XPR-4K XPR-8K Base XPR-4K XPR-8K Base XPR-4K XPR-8K 3DGS SSIM 0.87 0.87 0.87 0.85 0.85 0.85 0.97 0.97 0.97 LPIPS 0.182 0.182 0.182 0.169 0.169 0.169 0.030 0.030 0.030
> 3DGUT SSIM 0.87 0.87 0.87 0.86 0.85 0.85 0.97 0.97 0.97 LPIPS 0.184 0.184 0.184 0.168 0.169 0.168 0.030 0.030 0.030
> LinPrim SSIM 0.86 0.86 0.86 0.83 0.83 0.83 0.97 0.97 0.97 LPIPS 0.190 0.190 0.190 0.195 0.195 0.195 0.028 0.029 0.028

Table 7. Comparison of average SSIM and LPIPS of XPR with Base 

implementations for different point-based rendering methods and datasets on an L40 .

## A.3 Ablation Study 

We study how different hyperparameters of XPR affect performance across various devices. Fig. 8 depicts the average FPS of XPR across different rendering methods and devices ( L40 , MI210 , MAX1100 ) for the 360 dataset. First, we observe that increasing the number of bins increases FPS initially. This is because, with fewer bins, com-puting pixel colors requires more redundant computation beyond the actual number of primitives that the tiles in each bin intersect. For some pixel tiles, the blending loop must process more primi-tives without the ability to terminate early. As the number of bins increases, the FPS plateaus or even drops. Beyond the optimal bin count, shrinking per-bin batch sizes reduces SM occupancy and increases launch overheads faster than the additional bins reduce redundancy. Second, the batch divisor controls the granularity of early exit in the blend loop. A smaller batch divisor checks exit conditions less frequently, reducing per-iteration overhead at the cost of wasted work on already-saturated pixel colors. A larger batch divisor enables tighter early termination but adds loop over-head. Since both the number of bins and the batch divisor affect performance across scenes and hardware, XPR’s hyperparameter se-lection process sweeps these hyperparameters at compile time and chooses a performant configuration for a given scene and device. 1 2 4 6 8                                            

> 40
> 60
> 80
> 3DGS (L40 )
> 12468
> 40
> 60
> 3DGUT (L40 )
> 12468
> 20
> 40
> 60
> LinPrim (L40 )
> 12468
> 15
> 20
> 25
> FPS
> 3DGS (MI210 )
> 12468
> 10
> 15
> 20
> 3DGUT (MI210 )
> 12468
> 5
> 10
> 15
> 20
> LinPrim (MI210 )
> 12468
> 2.5
> 5.0
> 7.5
> 10.0
> 3DGS (MAX1100 )
> 12468
> Number of bins
> 2
> 4
> 6
> 3DGUT (MAX1100 )
> 12468
> 2.5
> 5.0
> 7.5
> LinPrim (MAX1100 )
> D=1 D=2 D=4 D=8

Fig. 8. Average rendering FPS (y-axis) vs. the number of bins (x-axis) with different batch divisors for the 360 dataset. 

13